<h1 id="headline" >Eat-Da-Burger!</h1>

<main>
  <div class="container">
    <div class="row">

      <div class="col s6">
        <p>This buger looks yummy!</p>
        {{#each burgers}} {{#unless this.devoured}}
        <div>
          <button class="notEaten" data-id="{{id}}" data-newDevour="{{devoured}}">
            {{this.burger_name}}
          </button>
        </div>
        {{/unless}} {{/each}}
      </div>

      <div class="col s6">
        <p>This burger has been DEVOURED!!!</p>

        {{#each burgers}} {{#if this.devoured}}
        
          <div>
            <button class="burgerEaten" data-id="{{id}}" data-newDevour="{{notDevoured}}">
            {{this.burger_name}}


            {{this.burger_name}}
          </div>
    
        {{/if}} {{/each}}
      </div>

    </div>
  </div>
</main>

<h2>Make Your Own Burger!</h2>

<div class="row">
  <div class="col s12">
    <form action="/burgers" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="Double Bacon Cheese Burger">
      <button type="submit" class="addBurgerButton">Add My Burger</button>
    </form>
  </div>
</div>