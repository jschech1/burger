<h1 id="headline" >Eat-Da-Burger!</h1>
<h3 id="h3" >Before he eats you!</h3>

<p id="make-text">Make Your Own Burger!</p>

<div class="row" id="make-field">
  <div class="col s12">
    <form action="/burgers" method="POST">
    <div>
      <input type="text" id="name" name="burger_name" placeholder="Double Bacon Cheese Burger">
      </div>
      <div>
      <button id="submit" type="submit" class="addBurgerButton">Add My Burger</button>
      </div>
    </form>
  </div>
</div>

<main>
  <div class="container">
    <div class="row">

      <div class="col s6">
        <p id="not-devoured-text">This buger is ready to eat!</p>
        {{#each burgers}} {{#unless this.devoured}}
        <div id="not-devoured-burgers">
          <button class="notEaten" data-id="{{id}}" data-newDevour="{{devoured}}">
            {{this.burger_name}}
          </button>
        </div>
        {{/unless}} {{/each}}
      </div>

      <div class="col s6">
        <p id="devoured-text">This burger has been DEVOURED!!!</p>

        {{#each burgers}} {{#if this.devoured}}
        
          <div>
            <button class="burgerEaten" data-id="{{id}}" data-newDevour="{{notDevoured}}">
            {{this.burger_name}}


         
          </div>
    
        {{/if}} {{/each}}
      </div>

    </div>
  </div>
</main>

